const Roles = {
  CARER: 'carer', // show only view schedule,
  CARER_SENIOR: 'carer_senior', // show only view schedule,
  MANAGER: 'manager', // invoicing
  OWNER: 'owner', // all allowed, with few limitation
  ADMIN: 'admin', // all allowed
};

export default Roles;
