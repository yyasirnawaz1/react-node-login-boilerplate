export const add = (req, res) => {
  // TODO: Create user
  // login user
  // req.login(req.body, () => {
  //   res.redirect('/dashboard');
  // });
  // debugging('register user');
  // res.send(req.body);
};
