// import axios, { Axios, AxiosError } from "axios";
// import { useQuery } from "react-query";
// import Config from "../utils/config";
// import { Claim, User } from "../types/user";

// // const useFetchUser = () => {
// //   return useQuery<Claim[], AxiosError>("user", () => axios.get(`${Config.baseURL}/account/getuser?slide=false`).then((resp) => resp.data));
// // };

// // export const useLoginUser = (data: User) => {
// //   return useQuery<Claim, AxiosError>("login-user", () => axios.post(`${Config.baseURL}/api/auth/login`, data).then((resp) => resp.data));
// // };
